module FDSSpec (fdsSpec) where

import qualified Data.Map.Strict as M
import Text.Namelist

fdsSpec :: NamelistSpec
fdsSpec = M.fromList
    [ ("HEAD", M.fromList
        [ ("CHID", PSString)
        , ("FYI", PSString)
        , ("TITLE", PSString)
        ])
    , ("MESH", M.fromList
        [ ("COLOR", PSString)
        , ("CYLINDRICAL", PSString)
        , ("EVACUATION", PSString)
        , ("EVAC_HUMANS", PSString)
        , ("EVAC_Z_OFFSET", PSString)
        , ("FYI", PSString)
        , ("ID", PSString)
        , ("IJK", PSArray)
        , ("LEVEL", PSString)
        , ("MPI_PROCESS", PSString)
        , ("MULT_ID", PSString)
        , ("RGB", PSString)
        , ("XB", PSArray)
        , ("N_THREADS", PSString)
        , ("PERIODIC_MESH_IDS", PSString)
        ])
    , ("TIME", M.fromList
        [ ("DT", PSString)
        , ("EVAC_DT_FLOWFIELD", PSString)
        , ("EVAC_DT_STEADY_STATE", PSString)
        , ("FYI", PSString)
        , ("LIMITING_DT_RATIO", PSString)
        , ("LOCK_TIME_STEP", PSString)
        , ("RESTRICT_TIME_STEP", PSString)
        , ("T_BEGIN", PSString)
        , ("T_END", PSDouble)
        , ("T_END_GEOM", PSString)
        , ("TIME_SHRINK_FACTOR", PSString)
        , ("WALL_INCREMENT", PSString)
        , ("WALL_INCREMENT_HT3D", PSString)
        , ("TWFIN", PSString)
        ])
    , ("MULT", M.fromList
        [ ("DX", PSString)
        , ("DXB", PSString)
        , ("DX0", PSString)
        , ("DY", PSString)
        , ("DY0", PSString)
        , ("DZ", PSString)
        , ("DZ0", PSString)
        , ("FYI", PSString)
        , ("ID", PSString)
        , ("I_LOWER", PSString)
        , ("I_UPPER", PSString)
        , ("J_LOWER", PSString)
        , ("J_UPPER", PSString)
        , ("K_LOWER", PSString)
        , ("K_UPPER", PSString)
        , ("N_LOWER", PSString)
        , ("N_UPPER", PSString)
        ])
    , ("MISC", M.fromList
        [ ("AGGLOMERATION", PSString)
        , ("AEROSOL_AL2O3", PSString)
        , ("ALLOW_SURFACE_PARTICLES", PSString)
        , ("ALLOW_UNDERSIDE_PARTICLES", PSString)
        , ("ASSUMED_GAS_TEMPERATURE", PSString)
        , ("ASSUMED_GAS_TEMPERATURE_RAMP", PSString)
        , ("BAROCLINIC", PSString)
        , ("BNDF_DEFAULT", PSString)
        , ("CC_IBM", PSString)
        , ("CNF_CUTOFF", PSString)
        , ("CFL_MAX", PSString)
        , ("CFL_MIN", PSString)
        , ("CFL_VELOCITY_NORM", PSString)
        , ("CHECK_HT", PSString)
        , ("CHECK_REALIZABILITY", PSString)
        , ("CHECK_VN", PSString)
        , ("CLIP_MASS_FRACTION", PSString)
        , ("COMPUTE_VISCOSITY_TWICE", PSString)
        , ("COMPUTE_ZETA_SOURCE_TERM", PSString)
        , ("CONSTANT_H_SOLID", PSString)
        , ("CONSTANT_SPECIFIC_HEAT_RATIO", PSString)
        , ("CORIOLIS_VECTOR", PSString)
        , ("CORRECT_SUBGRID_TEMPERATURE", PSString)
        , ("COUPLED_1D3D_HEAT_TRANSFER", PSString)
        , ("C_DEARDORFF", PSString)
        , ("C_RNG", PSString)
        , ("C_RNG_CUTOFF", PSString)
        , ("C_SMAGORINSKY", PSString)
        , ("C_VREMAN", PSString)
        , ("DNS", PSString)
        , ("DRAG_CFL_MAX", PSString)
        , ("DT_MEAN_FORCING", PSString)
        , ("ENTHALPY_TRANSPORT", PSString)
        , ("EQUILIBRIUM_MODEL", PSString)
        , ("EVACUATION_DRILL", PSString)
        , ("EVACUATION_MC_MODE", PSString)
        , ("EVAC_PRESSURE_ITERATIONS", PSString)
        , ("EVAC_SURF_DEFAULT", PSString)
        , ("EVAC_TIME_ITERATIONS", PSString)
        , ("EVAPORATION", PSString)
        , ("EVAP_2", PSString)
        , ("EXCHANGE_EDGES", PSString)
        , ("EXTERNAL_BOUNDARY_CORRECTION", PSString)
        , ("EXTINCTION_MODEL", PSString)
        , ("HVAC_PRES_RELAX", PSString)
        , ("HT3D_TEST", PSString)
        , ("FDS5_OPTIONS", PSString)
        , ("FLUX_LIMITER", PSString)
        , ("FORCE_VECTOR", PSString)
        , ("FREEZE_VELOCITY", PSString)
        , ("FYI", PSString)
        , ("GAMMA", PSString)
        , ("GRAVITATIONAL_DEPOSITION", PSString)
        , ("GRAVITATIONAL_SETTLING", PSString)
        , ("GROUND_LEVEL", PSString)
        , ("GVEC", PSString)
        , ("DT_HVAC", PSString)
        , ("H_F_REFERENCE_TEMPERATURE", PSString)
        , ("HRRPUV_MAX_SMV", PSString)
        , ("HUMIDITY", PSString)
        , ("HVAC_MASS_TRANSPORT", PSString)
        , ("IBLANK_SMV", PSString)
        , ("IMMERSED_BOUNDARY_METHOD", PSString)
        , ("INITIAL_UNMIXED_FRACTION", PSString)
        , ("KINETIC_ENERGY_SOURCE", PSString)
        , ("LAPSE_RATE", PSString)
        , ("LES_FILTER_WIDTH", PSString)
        , ("MAX_CHEMISTRY_ITERATIONS", PSString)
        , ("MAX_LEAK_PATHS", PSString)
        , ("MAXIMUM_VISIBILITY", PSString)
        , ("MEAN_FORCING", PSString)
        , ("MPI_TIMEOUT", PSString)
        , ("N_FIXED_CHEMISTRY_SUBSTEPS", PSString)
        , ("NEAR_WALL_TURBULENCE_MODEL", PSString)
        , ("NEW_MOMENTUM_NUDGING", PSString)
        , ("NEW_OPEN_BOUNDARY", PSString)
        , ("NOISE", PSString)
        , ("NOISE_VELOCITY", PSString)
        , ("NO_EVACUATION", PSString)
        , ("NO_RAMPS", PSString)
        , ("NORTHANGLE", PSString)
        , ("OVERWRITE", PSString)
        , ("PARTICLE_CFL_MAX", PSString)
        , ("PARTICLE_CFL_MIN", PSString)
        , ("PARTICLE_CFL", PSString)
        , ("PERIODIC_TEST", PSString)
        , ("PROFILING", PSString)
        , ("POROUS_FLOOR", PSString)
        , ("POTENTIAL_TEMPERATURE_CORRECTION", PSString)
        , ("PR", PSString)
        , ("PROCESS_ALL_MESHES", PSString)
        , ("PROJECTION", PSString)
        , ("P_INF", PSString)
        , ("RAMP_FVX_T", PSString)
        , ("RAMP_FVY_T", PSString)
        , ("RAMP_FVZ_T", PSString)
        , ("RAMP_GX", PSString)
        , ("RAMP_GY", PSString)
        , ("RAMP_GZ", PSString)
        , ("RAMP_U0", PSString)
        , ("RAMP_U0_T", PSString)
        , ("RAMP_V0", PSString)
        , ("RAMP_V0_T", PSString)
        , ("RAMP_W0", PSString)
        , ("RAMP_W0_T", PSString)
        , ("RAMP_U0_Z", PSString)
        , ("RAMP_V0_Z", PSString)
        , ("RAMP_W0_Z", PSString)
        , ("RADIATION", PSString)
        , ("RESEARCH_MODE", PSString)
        , ("RESTART", PSString)
        , ("RESTART_CHID", PSString)
        , ("RICHARDSON_ERROR_TOLERANCE", PSString)
        , ("RUN_AVG_FAC", PSString)
        , ("SC", PSString)
        , ("SECOND_ORDER_INTERPOLATED_BOUNDARY", PSString)
        , ("SECOND_ORDER_PARTICLE_TRANSPORT", PSString)
        , ("SHARED_FILE_SYSTEM", PSString)
        , ("SLIP_CONDITION", PSString)
        , ("SMOKE_ALBEDO", PSString)
        , ("SOLID_PHASE_ONLY", PSString)
        , ("SOOT_OXIDATION", PSString)
        , ("SPONGE_LAYER_DISTANCE", PSString)
        , ("STRATIFICATION", PSString)
        , ("SUPPRESSION", PSString)
        , ("SURF_DEFAULT", PSString)
        , ("TEMPERATURE_DEPENDENT_REACTION", PSString)
        , ("TENSOR_DIFFUSIVITY", PSString)
        , ("TERRAIN_CASE", PSString)
        , ("TERRAIN_IMAGE", PSString)
        , ("TEST_FILTER_QUADRATURE", PSString)
        , ("TEXTURE_ORIGIN", PSString)
        , ("THERMOPHORETIC_DEPOSITION", PSString)
        , ("THICKEN_OBSTRUCTIONS", PSString)
        , ("TRANSPORT_UNMIXED_FRACTION", PSString)
        , ("TRANSPORT_ZETA_SCHEME", PSString)
        , ("TMPA", PSString)
        , ("TURBULENCE_MODEL", PSString)
        , ("TURBULENT_DEPOSITION", PSString)
        , ("TURB_INIT_CLOCK", PSString)
        , ("U0", PSString)
        , ("UVW_FILE", PSString)
        , ("V0", PSString)
        , ("VEG_LEVEL_SET_COUPLED", PSString)
        , ("VEG_LEVEL_SET_UNCOUPLED", PSString)
        , ("VERBOSE", PSString)
        , ("VISIBILITY_FACTOR", PSString)
        , ("VN_MAX", PSString)
        , ("VN_MIN", PSString)
        , ("Y_CO2_INFTY", PSString)
        , ("Y_O2_INFTY", PSString)
        , ("W0", PSString)
        , ("WD_PROPS", PSString)
        , ("WIND_BOUNDARY", PSString)
        , ("WIND_ONLY", PSString)
        ])
    , ("DUMP", M.fromList
        [ ("CLIP_RESTART_FILES", PSString)
        , ("COLUMN_DUMP_LIMIT", PSString)
        , ("CTRL_COLUMN_LIMIT", PSString)
        , ("DEVC_COLUMN_LIMIT", PSString)
        , ("DT_BNDE", PSString)
        , ("DT_BNDF", PSString)
        , ("DT_CPU", PSString)
        , ("DT_CTRL", PSString)
        , ("DT_DEVC", PSString)
        , ("DT_DEVC_LINE", PSString)
        , ("DT_FLUSH", PSString)
        , ("DT_GEOM", PSString)
        , ("DT_HRR", PSString)
        , ("DT_ISOF", PSString)
        , ("DT_MASS", PSString)
        , ("DT_PART", PSString)
        , ("DT_PL3D", PSString)
        , ("DT_PROF", PSString)
        , ("DT_RESTART", PSString)
        , ("DT_SL3D", PSString)
        , ("DT_SLCF", PSString)
        , ("EB_PART_FILE", PSString)
        , ("FLUSH_FILE_BUFFERS", PSString)
        , ("GEOM_DIAG", PSString)
        , ("MASS_FILE", PSString)
        , ("MAXIMUM_PARTICLES", PSString)
        , ("MMS_TIMER", PSString)
        , ("NFRAMES", PSString)
        , ("PLOT3D_PART_ID", PSString)
        , ("PLOT3D_QUANTITY", PSString)
        , ("PLOT3D_SPEC_ID", PSString)
        , ("PLOT3D_SPEC_ID", PSString)
        , ("PLOT3D_VELO_INDEX", PSString)
        , ("RENDER_FILE", PSString)
        , ("SIG_FIGS", PSString)
        , ("SIG_FIGS_EXP", PSString)
        , ("SMOKE3D", PSString)
        , ("SMOKE3D_QUANTITY", PSString)
        , ("SMOKE3D_SPEC_ID", PSString)
        , ("STATUS_FILES", PSString)
        , ("SUPPRESS_DIAGNOSTICS", PSString)
        , ("UVW_TIMER", PSString)
        , ("VELOCITY_ERROR_FILE", PSString)
        , ("WRITE_XYZ", PSString)
        ])
    , ("SPEC", M.fromList
        [ ("AEROSOL", PSString)
        , ("BACKGROUND", PSString)
        , ("COPY_LUMPED", PSString)
        , ("CONDUCTIVITY", PSString)
        , ("CONDUCTIVITY_SOLID", PSString)
        , ("DENSITY_LIQUID", PSString)
        , ("DENSITY_SOLID", PSString)
        , ("DIFFUSIVITY", PSString)
        , ("ENTHALPY_OF_FORMATION", PSString)
        , ("EPSILONKLJ", PSString)
        , ("FIC_CONCENTRATION", PSString)
        , ("FLD_LETHAL_DOSE", PSString)
        , ("FORMULA", PSString)
        , ("FYI", PSString)
        , ("HEAT_OF_VAPORIZATION", PSString)
        , ("H_V_REFERENCE_TEMPERATURE", PSString)
        , ("ID", PSString)
        , ("LUMPED_COMPONENT_ONLY", PSString)
        , ("MASS_EXTINCTION_COEFFICIENT", PSString)
        , ("MASS_FRACTION", PSString)
        , ("MASS_FRACTION_0", PSString)
        , ("MAX_DIAMETER", PSString)
        , ("MEAN_DIAMETER", PSString)
        , ("MELTING_TEMPERATURE", PSString)
        , ("MIN_DIAMETER", PSString)
        , ("MW", PSString)
        , ("N_BINS", PSString)
        , ("PR_GAS", PSString)
        , ("PRIMITIVE", PSString)
        , ("RADCAL_ID", PSString)
        , ("RAMP_CP", PSString)
        , ("RAMP_CP_L", PSString)
        , ("RAMP_D", PSString)
        , ("RAMP_G_F", PSString)
        , ("RAMP_K", PSString)
        , ("RAMP_MU", PSString)
        , ("REFERENCE_ENTHALPY", PSString)
        , ("REFERENCE_TEMPERATURE", PSString)
        , ("SIGMALJ", PSString)
        , ("SPEC_ID", PSString)
        , ("SPECIFIC_HEAT", PSString)
        , ("SPECIFIC_HEAT_LIQUID", PSString)
        , ("VAPORIZATION_TEMPERATURE", PSString)
        , ("VISCOSITY", PSString)
        , ("VOLUME_FRACTION", PSString)
        ])
    , ("REAC", M.fromList
        [ ("A", PSString)
        , ("ALT_REAC_ID", PSString)
        , ("AUTO_IGNITION_TEMPERATURE", PSString)
        , ("C", PSString)
        , ("CHECK_ATOM_BALANCE", PSString)
        , ("CO_YIELD", PSString)
        , ("CRITICAL_FLAME_TEMPERATURE", PSString)
        , ("E", PSString)
        , ("EPUMO2", PSString)
        , ("K", PSString)
        , ("EQUATION", PSString)
        , ("FIXED_MIX_TIME", PSString)
        , ("FLAME_SPEED", PSString)
        , ("FLAME_SPEED_EXPONENT", PSString)
        , ("FLAME_SPEED_TEMPERATURE", PSString)
        , ("FORMULA", PSString)
        , ("FUEL", PSString)
        , ("FUEL_RADCAL_ID", PSString)
        , ("FWD_ID", PSString)
        , ("FYI", PSString)
        , ("H", PSString)
        , ("HEAT_OF_COMBUSTION", PSString)
        , ("ID", PSString)
        , ("IDEAL", PSString)
        , ("N", PSString)
        , ("NU", PSString)
        , ("N_S", PSString)
        , ("N_T", PSString)
        , ("O", PSString)
        , ("ODE_SOLVER", PSString)
        , ("RADIATIVE_FRACTION", PSString)
        , ("RAMP_CHI_R", PSString)
        , ("RAMP_FS", PSString)
        , ("REAC_ATOM_ERROR", PSString)
        , ("REAC_MASS_ERROR", PSString)
        , ("REVERSE", PSString)
        , ("SOOT_H_FRACTION", PSString)
        , ("SOOT_YIELD", PSString)
        , ("SPEC_ID_N_S", PSString)
        , ("SPEC_ID_NU", PSString)
        , ("TABLE_FS", PSString)
        , ("TAU_CHEM", PSString)
        , ("TAU_FLAME", PSString)
        , ("THIRD_BODY", PSString)
        , ("TURBULENT_FLAME_SPEED_ALPHA", PSString)
        , ("TURBULENT_FLAME_SPEED_EXPONENT", PSString)
        , ("Y_P_MIN_EDC", PSString)
        ])
    , ("PART", M.fromList
        [ ("AGE", PSString)
        , ("BREAKUP", PSString)
        , ("BREAKUP_CNF_RAMP_ID", PSString)
        , ("BREAKUP_DISTRIBUTION", PSString)
        , ("BREAKUP_GAMMA_D", PSString)
        , ("BREAKUP_RATIO", PSString)
        , ("BREAKUP_SIGMA_D", PSString)
        , ("CHECK_DISTRIBUTION", PSString)
        , ("CNF_RAMP_ID", PSString)
        , ("COLOR", PSString)
        , ("COMPLEX_REFRACTIVE_INDEX", PSString)
        , ("CTRL_ID", PSString)
        , ("DENSE_VOLUME_FRACTION", PSString)
        , ("DEVC_ID", PSString)
        , ("DIAMETER", PSString)
        , ("DISTRIBUTION", PSString)
        , ("DRAG_COEFFICIENT", PSString)
        , ("DRAG_LAW", PSString)
        , ("FREE_AREA_FRACTION", PSString)
        , ("FYI", PSString)
        , ("GAMMA_D", PSString)
        , ("HEAT_OF_COMBUSTION", PSString)
        , ("HORIZONTAL_VELOCITY", PSString)
        , ("ID", PSString)
        , ("INITIAL_TEMPERATURE", PSString)
        , ("MASSLESS", PSString)
        , ("MAXIMUM_DIAMETER", PSString)
        , ("MINIMUM_DIAMETER", PSString)
        , ("MONODISPERSE", PSString)
        , ("N_STRATA", PSString)
        , ("ORIENTATION", PSString)
        , ("PERMEABILITY", PSString)
        , ("PERIODIC_X", PSString)
        , ("PERIODIC_Y", PSString)
        , ("PERIODIC_Z", PSString)
        , ("POROUS_VOLUME_FRACTION", PSString)
        , ("PROP_ID", PSString)
        , ("QUANTITIES", PSString)
        , ("QUANTITIES_SPEC_ID", PSString)
        , ("RADIATIVE_PROPERTY_TABLE", PSString)
        , ("REAL_REFRACTIVE_INDEX", PSString)
        , ("RGB", PSString)
        , ("SAMPLING_FACTOR", PSString)
        , ("SIGMA_D", PSString)
        , ("SPEC_ID", PSString)
        , ("STATIC", PSString)
        , ("SURFACE_TENSION", PSString)
        , ("SURF_ID", PSString)
        , ("TARGET_ONLY", PSString)
        , ("TURBULENT_DISPERSION", PSString)
        , ("VERTICAL_VELOCITY", PSString)
        ])
    , ("PROP", M.fromList
        [ ("ACTIVATION_OBSCURATION", PSString)
        , ("ACTIVATION_TEMPERATURE", PSString)
        , ("ALPHA_C", PSString)
        , ("ALPHA_E", PSString)
        , ("BEAD_DENSITY", PSString)
        , ("BEAD_DIAMETER", PSString)
        , ("BEAD_EMISSIVITY", PSString)
        , ("BEAD_HEAT_TRANSFER_COEFFICIENT", PSString)
        , ("BEAD_SPECIFIC_HEAT", PSString)
        , ("BETA_C", PSString)
        , ("BETA_E", PSString)
        , ("FED_ACTIVITY", PSString)
        , ("CHARACTERISTIC_VELOCITY", PSString)
        , ("C_FACTOR", PSString)
        , ("FLOW_RAMP", PSString)
        , ("FLOW_RATE", PSString)
        , ("FLOW_TAU", PSString)
        , ("GAUGE_EMISSIVITY", PSString)
        , ("GAUGE_TEMPERATURE", PSString)
        , ("ID", PSString)
        , ("INITIAL_TEMPERATURE", PSString)
        , ("K_FACTOR", PSString)
        , ("LENGTH", PSString)
        , ("MASS_FLOW_RATE", PSString)
        , ("OFFSET", PSString)
        , ("OPERATING_PRESSURE", PSString)
        , ("ORIFICE_DIAMETER", PSString)
        , ("P0", PSString)
        , ("PARTICLES_PER_SECOND", PSString)
        , ("PARTICLE_VELOCITY", PSString)
        , ("PART_ID", PSString)
        , ("PDPA_END", PSString)
        , ("PDPA_HISTOGRAM", PSString)
        , ("PDPA_HISTOGRAM_LIMITS", PSString)
        , ("PDPA_HISTOGRAM_NBINS", PSString)
        , ("PDPA_HISTOGRAM_CUMULATIVE", PSString)
        , ("PDPA_INTEGRATE", PSString)
        , ("PDPA_M", PSString)
        , ("PDPA_N", PSString)
        , ("PDPA_NORMALIZE", PSString)
        , ("PDPA_RADIUS", PSString)
        , ("PDPA_START", PSString)
        , ("PRESSURE_RAMP", PSString)
        , ("PX", PSString)
        , ("PXX", PSString)
        , ("QUANTITY", PSString)
        , ("RTI", PSString)
        , ("SMOKEVIEW_ID", PSString)
        , ("SMOKEVIEW_PARAMETERS", PSString)
        , ("SPEC_ID", PSString)
        , ("SPRAY_ANGLE", PSString)
        , ("SPRAY_PATTERN_BETA", PSString)
        , ("SPRAY_PATTERN_MU", PSString)
        , ("SPRAY_PATTERN_SHAPE", PSString)
        , ("SPRAY_PATTERN_TABLE", PSString)
        , ("VELOCITY_COMPONENT", PSString)
        , ("DROPLET_VELOCITY", PSString)
        ])
    , ("MATL", M.fromList
        [ ("A", PSString)
        , ("ABSORPTION_COEFFICIENT", PSString)
        , ("BOILING_TEMPERATURE", PSString)
        , ("COLOR", PSString)
        , ("CONDUCTIVITY", PSString)
        , ("CONDUCTIVITY_RAMP", PSString)
        , ("DENSITY", PSString)
        , ("E", PSString)
        , ("EMISSIVITY", PSString)
        , ("FYI", PSString)
        , ("HEATING_RATE", PSString)
        , ("HEAT_OF_COMBUSTION", PSString)
        , ("HEAT_OF_REACTION", PSString)
        , ("ID", PSString)
        , ("MATL_ID", PSString)
        , ("NU_MATL", PSString)
        , ("NU_SPEC", PSString)
        , ("N_REACTIONS", PSString)
        , ("N_S", PSString)
        , ("N_T", PSString)
        , ("N_O2", PSString)
        , ("PCR", PSString)
        , ("PYROLYSIS_RANGE", PSString)
        , ("REFERENCE_RATE", PSString)
        , ("REFERENCE_TEMPERATURE", PSString)
        , ("RGB", PSString)
        , ("SPECIFIC_HEAT", PSString)
        , ("SPECIFIC_HEAT_RAMP", PSString)
        , ("SPEC_ID", PSString)
        , ("THRESHOLD_SIGN", PSString)
        , ("THRESHOLD_TEMPERATURE", PSString)
        , ("POROSITY", PSString)
        , ("ALLOW_SHRINKING", PSString)
        , ("ALLOW_SWELLING", PSString)
        , ("GAS_DIFFUSION_DEPTH", PSString)
        ])
    , ("SURF", M.fromList
        [ ("ADIABATIC", PSString)
        , ("AUTO_IGNITION_TEMPERATURE", PSString)
        , ("BACKING", PSString)
        , ("BURN_AWAY", PSString)
        , ("CELL_SIZE_FACTOR", PSString)
        , ("C_FORCED_CONSTANT", PSString)
        , ("C_FORCED_PR_EXP", PSString)
        , ("C_FORCED_RE", PSString)
        , ("C_FORCED_RE_EXP", PSString)
        , ("C_HORIZONTAL", PSString)
        , ("C_VERTICAL", PSString)
        , ("COLOR", PSString)
        , ("CONVECTION_LENGTH_SCALE", PSString)
        , ("CONVECTIVE_HEAT_FLUX", PSString)
        , ("CONVERT_VOLUME_TO_MASS", PSString)
        , ("DEFAULT", PSString)
        , ("DT_INSERT", PSString)
        , ("EMISSIVITY", PSString)
        , ("EMISSIVITY_BACK", PSString)
        , ("EVAC_DEFAULT", PSString)
        , ("EXTERNAL_FLUX", PSString)
        , ("E_COEFFICIENT", PSString)
        , ("FIRELINE_MLR_MAX", PSString)
        , ("FREE_SLIP", PSString)
        , ("FYI", PSString)
        , ("GEOMETRY", PSString)
        , ("HEAT_OF_VAPORIZATION", PSString)
        , ("HEAT_TRANSFER_COEFFICIENT", PSString)
        , ("HEAT_TRANSFER_COEFFICIENT_BACK", PSString)
        , ("HEAT_TRANSFER_MODEL", PSString)
        , ("HRRPUA", PSString)
        , ("HT3D", PSString)
        , ("ID", PSString)
        , ("IGNITION_TEMPERATURE", PSString)
        , ("INNER_RADIUS", PSString)
        , ("INTERNAL_HEAT_SOURCE", PSString)
        , ("LAYER_DIVIDE", PSString)
        , ("LEAK_PATH", PSString)
        , ("LENGTH", PSString)
        , ("MASS_FLUX", PSString)
        , ("MASS_FLUX_TOTAL", PSString)
        , ("MASS_FLUX_VAR", PSString)
        , ("MASS_FRACTION", PSString)
        , ("MASS_TRANSFER_COEFFICIENT", PSString)
        , ("MATL_ID", PSString)
        , ("MATL_MASS_FRACTION", PSString)
        , ("MINIMUM_LAYER_THICKNESS", PSString)
        , ("MLRPUA", PSString)
        , ("N_CELLS_MAX", PSString)
        , ("N_LAYER_CELLS_MAX", PSString)
        , ("NET_HEAT_FLUX", PSString)
        , ("NO_SLIP", PSString)
        , ("NPPC", PSString)
        , ("PARTICLE_MASS_FLUX", PSString)
        , ("PART_ID", PSString)
        , ("PLE", PSString)
        , ("PROFILE", PSString)
        , ("RADIUS", PSString)
        , ("RAMP_EF", PSString)
        , ("RAMP_MF", PSString)
        , ("RAMP_PART", PSString)
        , ("RAMP_Q", PSString)
        , ("RAMP_T", PSString)
        , ("RAMP_T_I", PSString)
        , ("RAMP_V", PSString)
        , ("RAMP_V_X", PSString)
        , ("RAMP_V_Y", PSString)
        , ("RAMP_V_Z", PSString)
        , ("RGB", PSString)
        , ("ROUGHNESS", PSString)
        , ("SPEC_ID", PSString)
        , ("SPREAD_RATE", PSString)
        , ("STRETCH_FACTOR", PSString)
        , ("TAU_EF", PSString)
        , ("TAU_MF", PSString)
        , ("TAU_PART", PSString)
        , ("TAU_Q", PSString)
        , ("TAU_T", PSString)
        , ("TAU_V", PSString)
        , ("TEXTURE_HEIGHT", PSString)
        , ("TEXTURE_MAP", PSString)
        , ("TEXTURE_WIDTH", PSString)
        , ("TGA_ANALYSIS", PSString)
        , ("TGA_FINAL_TEMPERATURE", PSString)
        , ("TGA_HEATING_RATE", PSString)
        , ("THICKNESS", PSString)
        , ("TMP_BACK", PSString)
        , ("TMP_FRONT", PSString)
        , ("TMP_INNER", PSString)
        , ("TRANSPARENCY", PSString)
        , ("VEGETATION", PSString)
        , ("VEGETATION_ARRHENIUS_DEGRAD", PSString)
        , ("VEGETATION_CDRAG", PSString)
        , ("VEGETATION_CHAR_FRACTION", PSString)
        , ("VEGETATION_ELEMENT_DENSITY", PSString)
        , ("VEGETATION_GROUND_TEMP", PSString)
        , ("VEGETATION_HEIGHT", PSString)
        , ("VEGETATION_INITIAL_TEMP", PSString)
        , ("VEGETATION_LAYERS", PSString)
        , ("VEGETATION_LINEAR_DEGRAD", PSString)
        , ("VEGETATION_LOAD", PSString)
        , ("VEGETATION_LSET_IGNITE_TIME", PSString)
        , ("VEG_LSET_QCON", PSString)
        , ("VEGETATION_MOISTURE", PSString)
        , ("VEGETATION_NO_BURN", PSString)
        , ("VEGETATION_SVRATIO", PSString)
        , ("VEG_LEVEL_SET_SPREAD", PSString)
        , ("VEG_LSET_ROS_BACK", PSString)
        , ("VEG_LSET_ROS_FLANK", PSString)
        , ("VEG_LSET_ROS_HEAD", PSString)
        , ("VEG_LSET_WIND_EXP", PSString)
        , ("VEG_LSET_SIGMA", PSString)
        , ("VEG_LSET_HT", PSString)
        , ("VEG_LSET_BETA", PSString)
        , ("VEG_LSET_ELLIPSE", PSString)
        , ("VEG_LSET_TAN2", PSString)
        , ("VEG_LSET_ELLIPSE_HEAD", PSString)
        , ("VEL", PSDouble)
        , ("VEL_BULK", PSString)
        , ("VEL_GRAD", PSString)
        , ("VEL_T", PSString)
        , ("VOLUME_FLOW", PSString)
        , ("WIDTH", PSString)
        , ("XYZ", PSString)
        , ("Z0", PSString)
        , ("ZETA_FRONT", PSString)
        , ("EXTERNAL_FLUX_RAMP", PSString)
        , ("TAU_EXTERNAL_FLUX", PSString)
        , ("VOLUME_FLUX", PSString)
        ])
    , ("PRES", M.fromList
        [ ("CHECK_POISSON", PSString)
        , ("FISHPAK_BC", PSString)
        , ("GLMAT_SOLVER", PSString)
        , ("ITERATION_SUSPEND_FACTOR", PSString)
        , ("LAPLACE_PRESSURE_CORRECTION", PSString)
        , ("MAX_PRESSURE_ITERATIONS", PSInt)
        , ("PRESSURE_RELAX_TIME", PSString)
        , ("PRESSURE_TOLERANCE", PSString)
        , ("RELAXATION_FACTOR", PSString)
        , ("SCARC_METHOD", PSString)
        , ("SCARC_KRYLOV", PSString)
        , ("SCARC_MULTIGRID", PSString)
        , ("SCARC_SMOOTH", PSString)
        , ("SCARC_PRECON", PSString)
        , ("SCARC_COARSE", PSString)
        , ("SCARC_INITIAL", PSString)
        , ("SCARC_ACCURACY", PSString)
        , ("SCARC_DEBUG", PSString)
        , ("SCARC_MULTIGRID_CYCLE", PSString)
        , ("SCARC_MULTIGRID_LEVEL", PSString)
        , ("SCARC_MULTIGRID_COARSENING", PSString)
        , ("SCARC_MULTIGRID_ITERATIONS", PSString)
        , ("SCARC_MULTIGRID_ACCURACY", PSString)
        , ("SCARC_MULTIGRID_INTERPOL", PSString)
        , ("SCARC_KRYLOV_ITERATIONS", PSString)
        , ("SCARC_KRYLOV_ACCURACY", PSString)
        , ("SCARC_SMOOTH_ITERATIONS", PSString)
        , ("SCARC_SMOOTH_ACCURACY", PSString)
        , ("SCARC_SMOOTH_OMEGA", PSString)
        , ("SCARC_PRECON_ITERATIONS", PSString)
        , ("SCARC_PRECON_ACCURACY", PSString)
        , ("SCARC_PRECON_OMEGA", PSString)
        , ("SCARC_COARSE_ITERATIONS", PSString)
        , ("SCARC_COARSE_ACCURACY", PSString)
        , ("SUSPEND_PRESSURE_ITERATIONS", PSString)
        , ("VELOCITY_TOLERANCE", PSDouble)
        ])
    , ("RADI", M.fromList
        [ ("ANGLE_INCREMENT", PSString)
        , ("BAND_LIMITS", PSString)
        , ("C_MAX", PSString)
        , ("C_MIN", PSString)
        , ("INITIAL_RADIATION_ITERATIONS", PSString)
        , ("KAPPA0", PSString)
        , ("NMIEANG", PSString)
        , ("NUMBER_RADIATION_ANGLES", PSString)
        , ("PATH_LENGTH", PSString)
        , ("RADIATION", PSString)
        , ("RADIATION_ITERATIONS", PSString)
        , ("RADIATIVE_FRACTION", PSString)
        , ("RADTMP", PSString)
        , ("RTE_SOURCE_CORRECTION", PSString)
        , ("TIME_STEP_INCREMENT", PSString)
        , ("WIDE_BAND_MODEL", PSString)
        , ("MIE_MINIMUM_DIAMETER", PSString)
        , ("MIE_MAXIMUM_DIAMETER", PSString)
        , ("MIE_NDG", PSString)
        , ("NUMBER_INITIAL_ITERATIONS", PSString)
        , ("QR_CLIP", PSString)
        ])
    , ("CLIP", M.fromList
        [ ("FYI", PSString)
        , ("MAXIMUM_DENSITY", PSString)
        , ("MAXIMUM_TEMPERATURE", PSString)
        , ("MINIMUM_DENSITY", PSString)
        , ("MINIMUM_TEMPERATURE", PSString)
        ])
    , ("RAMP", M.fromList
        [ ("CTRL_ID", PSString)
        , ("DEVC_ID", PSString)
        , ("F", PSString)
        , ("FYI", PSString)
        , ("ID", PSString)
        , ("NUMBER_INTERPOLATION_POINTS", PSString)
        , ("T", PSString)
        , ("X", PSString)
        , ("Z", PSString)
        ])
    , ("TABL", M.fromList
        [ ("FYI", PSString)
        , ("ID", PSString)
        , ("TABLE_DATA", PSString)
        ])
    , ("OBST", M.fromList
        [ ("ALLOW_VENT", PSString)
        , ("BNDF_FACE", PSString)
        , ("BNDF_OBST", PSString)
        , ("BULK_DENSITY", PSString)
        , ("COLOR", PSString)
        , ("CTRL_ID", PSString)
        , ("DEVC_ID", PSString)
        , ("EVACUATION", PSString)
        , ("FYI", PSString)
        , ("HT3D", PSString)
        , ("ID", PSString)
        , ("MATL_ID", PSString)
        , ("MESH_ID", PSString)
        , ("MULT_ID", PSString)
        , ("NOTERRAIN", PSString)
        , ("OUTLINE", PSString)
        , ("OVERLAY", PSString)
        , ("PERMIT_HOLE", PSString)
        , ("PROP_ID", PSString)
        , ("REMOVABLE", PSString)
        , ("RGB", PSString)
        , ("SURF_ID", PSString)
        , ("SURF_ID6", PSString)
        , ("SURF_IDS", PSString)
        , ("TEXTURE_ORIGIN", PSString)
        , ("THICKEN", PSString)
        , ("TRANSPARENCY", PSString)
        , ("XB", PSArray)
        ])
    , ("HOLE", M.fromList
        [ ("COLOR", PSString)
        , ("CTRL_ID", PSString)
        , ("DEVC_ID", PSString)
        , ("EVACUATION", PSString)
        , ("FYI", PSString)
        , ("ID", PSString)
        , ("MESH_ID", PSString)
        , ("MULT_ID", PSString)
        , ("RGB", PSString)
        , ("TRANSPARENCY", PSString)
        , ("XB", PSArray)
        ])
    , ("VENT", M.fromList
        [ ("COLOR", PSString)
        , ("CTRL_ID", PSString)
        , ("DEVC_ID", PSString)
        , ("DYNAMIC_PRESSURE", PSString)
        , ("EVACUATION", PSString)
        , ("FYI", PSString)
        , ("ID", PSString)
        , ("IOR", PSString)
        , ("L_EDDY", PSString)
        , ("L_EDDY_IJ", PSString)
        , ("MB", PSString)
        , ("MESH_ID", PSString)
        , ("MULT_ID", PSString)
        , ("N_EDDY", PSString)
        , ("OUTLINE", PSBool)
        , ("PBX", PSString)
        , ("PBY", PSString)
        , ("PBZ", PSString)
        , ("PRESSURE_RAMP", PSString)
        , ("RADIUS", PSString)
        , ("REYNOLDS_STRESS", PSString)
        , ("RGB", PSString)
        , ("SPREAD_RATE", PSString)
        , ("SURF_ID", PSString)
        , ("TEXTURE_ORIGIN", PSString)
        , ("TMP_EXTERIOR", PSString)
        , ("TMP_EXTERIOR_RAMP", PSString)
        , ("TRANSPARENCY", PSString)
        , ("UVW", PSString)
        , ("VEL_RMS", PSString)
        , ("WIND", PSString)
        , ("XB", PSArray)
        , ("XYZ", PSString)
        ])
    , ("INIT", M.fromList
        [ ("AUTO_IGNITION_TEMPERATURE", PSString)
        , ("CELL_CENTERED", PSString)
        , ("CTRL_ID", PSString)
        , ("DENSITY", PSString)
        , ("DEVC_ID", PSString)
        , ("DIAMETER", PSString)
        , ("DT_INSERT", PSString)
        , ("DX", PSString)
        , ("DY", PSString)
        , ("DZ", PSString)
        , ("HEIGHT", PSString)
        , ("HRRPUV", PSString)
        , ("ID", PSString)
        , ("MASS_FRACTION", PSString)
        , ("MASS_PER_TIME", PSString)
        , ("MASS_PER_VOLUME", PSString)
        , ("MULT_ID", PSString)
        , ("N_PARTICLES", PSString)
        , ("N_PARTICLES_PER_CELL", PSString)
        , ("PART_ID", PSString)
        , ("RADIUS", PSString)
        , ("SHAPE", PSString)
        , ("SPEC_ID", PSString)
        , ("TEMPERATURE", PSString)
        , ("UVW", PSString)
        , ("XB", PSArray)
        , ("XYZ", PSString)
        , ("PARTICLE_WEIGHT_FACTOR", PSString)
        , ("NUMBER_INITIAL_PARTICLES", PSString)
        ])
    , ("ZONE", M.fromList
        [ ("ID", PSString)
        , ("LEAK_AREA", PSString)
        , ("LEAK_PRESSURE_EXPONENT", PSString)
        , ("LEAK_REFERENCE_PRESSURE", PSString)
        , ("XB", PSArray)
        , ("PERIODIC", PSString)
        ])
    , ("DEVC", M.fromList
        [ ("BYPASS_FLOWRATE", PSString)
        , ("CONVERSION_FACTOR", PSString)
        , ("COORD_FACTOR", PSString)
        , ("CTRL_ID", PSString)
        , ("DELAY", PSString)
        , ("DEPTH", PSString)
        , ("DEVC_ID", PSString)
        , ("DRY", PSString)
        , ("DUCT_ID", PSString)
        , ("EVACUATION", PSString)
        , ("FLOWRATE", PSString)
        , ("FYI", PSString)
        , ("GHOST_CELL_IOR", PSString)
        , ("HIDE_COORDINATES", PSString)
        , ("ID", PSString)
        , ("INITIAL_STATE", PSString)
        , ("INIT_ID", PSString)
        , ("IOR", PSString)
        , ("LATCH", PSString)
        , ("MATL_ID", PSString)
        , ("NODE_ID", PSString)
        , ("NO_UPDATE_DEVC_ID", PSString)
        , ("NO_UPDATE_CTRL_ID", PSString)
        , ("ORIENTATION", PSString)
        , ("ORIENTATION_NUMBER", PSString)
        , ("OUTPUT", PSString)
        , ("PART_ID", PSString)
        , ("PIPE_INDEX", PSString)
        , ("POINTS", PSString)
        , ("PROP_ID", PSString)
        , ("QUANTITY", PSString)
        , ("QUANTITY2", PSString)
        , ("QUANTITY_RANGE", PSString)
        , ("R_ID", PSString)
        , ("REAC_ID", PSString)
        , ("RELATIVE", PSString)
        , ("ROTATION", PSString)
        , ("SETPOINT", PSString)
        , ("SMOOTHING_FACTOR", PSString)
        , ("SPEC_ID", PSString)
        , ("STATISTICS", PSString)
        , ("STATISTICS_START", PSString)
        , ("SURF_ID", PSString)
        , ("TIME_AVERAGED", PSString)
        , ("TIME_HISTORY", PSString)
        , ("TRIP_DIRECTION", PSString)
        , ("UNITS", PSString)
        , ("VELO_INDEX", PSString)
        , ("XB", PSArray)
        , ("XYZ", PSString)
        , ("X_ID", PSString)
        , ("Y_ID", PSString)
        , ("Z_ID", PSString)
        ])
    , ("CTRL", M.fromList
        [ ("CONSTANT", PSString)
        , ("CYCLES", PSString)
        , ("CYCLE_TIME", PSString)
        , ("DELAY", PSString)
        , ("DIFFERENTIAL_GAIN", PSString)
        , ("EVACUATION", PSString)
        , ("FUNCTION_TYPE", PSString)
        , ("ID", PSString)
        , ("INITIAL_STATE", PSString)
        , ("INTEGRAL_GAIN", PSString)
        , ("INPUT_ID", PSString)
        , ("LATCH", PSString)
        , ("N", PSString)
        , ("ON_BOUND", PSString)
        , ("PROPORTIONAL_GAIN", PSString)
        , ("RAMP_ID", PSString)
        , ("SETPOINT", PSString)
        , ("TARGET_VALUE", PSString)
        , ("TRIP_DIRECTION", PSString)
        ])
    , ("PROF", M.fromList
        [ ("FORMAT_INDEX", PSString)
        , ("FYI", PSString)
        , ("ID", PSString)
        , ("IOR", PSString)
        , ("QUANTITY", PSString)
        , ("XYZ", PSString)
        ])
    , ("ISOF", M.fromList
        [ ("FYI", PSString)
        , ("QUANTITY", PSString)
        , ("SPEC_ID", PSString)
        , ("VALUE", PSString)
        , ("VELO_INDEX", PSString)
        ])
    , ("SLCF", M.fromList
        [ ("AGL_SLICE", PSString)
        , ("CELL_CENTERED", PSBool)
        , ("EVACUATION", PSString)
        , ("FACE_CENTERED", PSString)
        , ("FIRE_LINE", PSString)
        , ("FYI", PSString)
        , ("ID", PSString)
        , ("IOR", PSString)
        , ("LEVEL_SET_FIRE_LINE", PSString)
        , ("MAXIMUM_VALUE", PSString)
        , ("MESH_NUMBER", PSString)
        , ("MINIMUM_VALUE", PSString)
        , ("PART_ID", PSString)
        , ("PBX", PSDouble)
        , ("PBY", PSDouble)
        , ("PBZ", PSDouble)
        , ("PROP_ID", PSString)
        , ("QUANTITY", PSString)
        , ("QUANTITY2", PSString)
        , ("REAC_ID", PSString)
        , ("SLICETYPE", PSString)
        , ("SPEC_ID", PSString)
        , ("VECTOR", PSBool)
        , ("VELO_INDEX", PSString)
        , ("XB", PSArray)
        ])
    , ("BNDF", M.fromList
        [ ("CELL_CENTERED", PSString)
        , ("FYI", PSString)
        , ("PART_ID", PSString)
        , ("PROP_ID", PSString)
        , ("QUANTITY", PSString)
        , ("SPEC_ID", PSString)
        , ("STATISTICS", PSString)
        ])
    , ("BNDE", M.fromList
        [ ("CELL_CENTERED", PSString)
        , ("FYI", PSString)
        , ("PART_ID", PSString)
        , ("PROP_ID", PSString)
        , ("QUANTITY", PSString)
        , ("SPEC_ID", PSString)
        ])
    , ("HVAC", M.fromList
        [ ("AIRCOIL_ID", PSString)
        , ("AMBIENT", PSString)
        , ("AREA", PSString)
        , ("CLEAN_LOSS", PSString)
        , ("COOLANT_SPECIFIC_HEAT", PSString)
        , ("COOLANT_MASS_FLOW", PSString)
        , ("COOLANT_TEMPERATURE", PSString)
        , ("CTRL_ID", PSString)
        , ("DAMPER", PSString)
        , ("DEVC_ID", PSString)
        , ("DIAMETER", PSString)
        , ("DUCT_ID", PSString)
        , ("DUCT_INTERP_TYPE", PSString)
        , ("EFFICIENCY", PSString)
        , ("FAN_ID", PSString)
        , ("FILTER_ID", PSString)
        , ("FIXED_Q", PSString)
        , ("ID", PSString)
        , ("LEAK_ENTHALPY", PSString)
        , ("LENGTH", PSString)
        , ("LOADING", PSString)
        , ("LOADING_MULTIPLIER", PSString)
        , ("LOSS", PSString)
        , ("MASS_FLOW", PSString)
        , ("MAX_FLOW", PSString)
        , ("MAX_PRESSURE", PSString)
        , ("N_CELLS", PSString)
        , ("NODE_ID", PSString)
        , ("PERIMETER", PSString)
        , ("RAMP_ID", PSString)
        , ("RAMP_LOSS", PSString)
        , ("REVERSE", PSString)
        , ("ROUGHNESS", PSString)
        , ("SPEC_ID", PSString)
        , ("TAU_AC", PSString)
        , ("TAU_FAN", PSString)
        , ("TAU_VF", PSString)
        , ("TYPE_ID", PSString)
        , ("VENT_ID", PSString)
        , ("VENT2_ID", PSString)
        , ("VOLUME_FLOW", PSString)
        , ("XYZ", PSString)
        ])
    , ("GEOM", M.fromList
        [ ("AUTO_TEXTURE", PSString)
        , ("AZIM", PSString)
        , ("AZIM_DOT", PSString)
        , ("COMPONENT_ONLY", PSString)
        , ("CUTCELLS", PSString)
        , ("DAZIM", PSString)
        , ("DELEV", PSString)
        , ("DSCALE", PSString)
        , ("DT_BNDC", PSString)
        , ("DT_GEOC", PSString)
        , ("DXYZ0", PSString)
        , ("DXYZ", PSString)
        , ("ELEV", PSString)
        , ("ELEV_DOT", PSString)
        , ("FACES", PSString)
        , ("GAXIS", PSString)
        , ("GEOM_IDS", PSString)
        , ("GROTATE", PSString)
        , ("GROTATE_DOT", PSString)
        , ("ID", PSString)
        , ("IJK", PSArray)
        , ("MATL_ID", PSString)
        , ("N_LAT", PSString)
        , ("N_LEVELS", PSString)
        , ("N_LONG", PSString)
        , ("SCALE", PSString)
        , ("SCALE_DOT", PSString)
        , ("SPHERE_ORIGIN", PSString)
        , ("SPHERE_RADIUS", PSString)
        , ("SPHERE_TYPE", PSString)
        , ("SURF_ID", PSString)
        , ("TEXTURE_MAPPING", PSString)
        , ("TEXTURE_ORIGIN", PSString)
        , ("TEXTURE_SCALE", PSString)
        , ("VERTS", PSString)
        , ("VOLUS", PSString)
        , ("XB", PSArray)
        , ("XYZ0", PSString)
        , ("XYZ", PSString)
        , ("XYZ_DOT", PSString)
        , ("ZMIN", PSString)
        , ("ZVALS", PSString)
        , ("BNDC_FILENAME", PSString)
        , ("GEOC_FILENAME", PSString)
        ])
    , ("TRNX", M.fromList
        [ ("CC", PSString)
        , ("FYI", PSString)
        , ("IDERIV", PSString)
        , ("MESH_NUMBER", PSString)
        , ("PC", PSString)
        ])
    , ("TRNY", M.fromList
        [ ("CC", PSString)
        , ("FYI", PSString)
        , ("IDERIV", PSString)
        , ("MESH_NUMBER", PSString)
        , ("PC", PSString)
        ])
    , ("TRNZ", M.fromList
        [ ("CC", PSString)
        , ("FYI", PSString)
        , ("IDERIV", PSString)
        , ("MESH_NUMBER", PSString)
        , ("PC", PSString)
        ])
    ]